#include <stdio.h>

void int_swap(int *a, int *b);

int main(void)
{
    int n;
    printf("Enter array size:\n");
    scanf("%d", &n);

    int arr[n];
    printf("Enter integer array:\n");
    for (int i = 0; i < n; ++i)
    {
        scanf("%d", &arr[n]);
    }

    // Selective sort.
    for (int i = 0; i < n; ++i)
    {
        
    }
}